[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "contractor-os"

[services.variables]
PORT = "3000"
